# 이 폴더에서 일하는 AI 에게

이 저장소는 **BSP 리빗 플랫폼 SDK** 다. 클라이언트 쪽(상주 매니저 + 리빗 점화 애드온)은
이미 만들어져 있고 동작이 확인되었다. 남은 일은 **서버** 와 **기존 애드온 적응** 이다.

## 사용자가 «붙여줘 / 설치해줘 / 세팅해줘» 라고 하면

- 이미 자기 플랫폼(애드인)이 있다 → `AI\ATTACH.md`
- 아직 없다 / 환경부터 맞춰야 한다 → `AI\SETUP.md`

둘 다 **한 번에 하나씩 묻는다.** 사용자 코드는 먼저 읽고, 넣을 자리를 합의한 뒤에 고친다.

## 먼저 읽을 것 (순서대로)

1. `PROPOSAL.md` — 무엇을 주고 무엇을 맡기는가
2. `SPEC\LAUNCHER.md` — **런처 최소기능 공통설계.** 여기 없는 기능은 런처의 일이 아니다
3. `SPEC\PROCESS.md` — 프로세스 두 개 구조. 왜 리빗 밖에서 도는가
4. `SPEC\CALLBACKS.md` — 파일·HTTP 규격 (여기가 실제 계약이다)
5. `SPEC\CONTRACT.md` — 도구 계약 (애드온을 만들 때만)
6. `SPEC\ADAPTER.md` — 서버가 **남의 것**일 때 붙이는 법 (거의 항상 이 경우다)
7. `SPEC\API.md` — **참조본**. 우리 파이썬 서버가 이렇게 생겼다는 것일 뿐, 따라야 할 의무는 없다

## 일감 (하나 골라 그대로 실행한다)

| 시킬 일 | 지시서 |
|---|---|
| **이미 있는 내 플랫폼 코드에 붙인다 (여기서 시작)** | `AI\ATTACH.md` — 네 줄 · 3단계 이관 · 리빗 실기 확인 |
| 코어가 이 PC 에서 도는지부터 | `binsp-probe.exe cycle` (리빗·서버·코드수정 없이 7줄 판정) |
| 지금 구조를 런처 형식으로 바꾼다 (플랫폼이 없을 때) | `AI\MIGRATE.md` — 6단계 |
| 기존 애드온(HTS 등)을 플랫폼에 태운다 | `AI\ADAPT_HTS.md` (전환 5단계에서 부른다) |
| 서버를 만든다 (언어·틀 자유) | `AI\SERVER.md` (전환 6단계) |
| 확인·시험 | `AI\TEST.md` · `AI\selftest.ps1` · `AI\healthcheck.ps1` · `AI\snapshot.ps1` |

두 지시서는 **끝에 «합격 시험» 이 붙어 있다.** 사람 눈이 아니라 그 시험으로 판정한다.
시험을 돌리기 전에는 «됐다» 고 말하지 않는다.

## 절대 규칙

1. `bin\` 안의 실행 파일과 `SPEC\` 의 규격은 **고치지 않는다.** 서버가 규격에 맞춘다
2. 남의 제품 폴더·설정 파일에 쓰지 않는다 (실제 사고 : 남의 `products.txt` 를 썼다가 도구 33개가 전부 사라졌다)
3. 선언한 리빗 연도와 실제 참조한 `RevitAPI` 버전이 다르면 안 된다
   (실제 사고 : 2021 로 빌드한 도구가 2024 에서 **버튼 누르는 순간** 죽었다 — `BuiltInCategory` 가 64비트로 바뀌었다)
4. 파일은 **SHA-256 으로만** 신뢰한다. 크기·이름·날짜로 판단하지 않는다
5. 안 되는 것을 된다고 적지 않는다. 못 한 것은 «못 했다» 로 남긴다

## 환경 메모

- 런처·애드온은 .NET Framework 4.8 (Revit ≤2024). 2025 이상은 .NET 8 별도 빌드
- 상태 폴더 : `%AppData%\BSP\Platform\`
- 런처 명령 : `bsp-launcher.exe status | sync | watch-revit | log | tail | enable <id> | disable <id>`
