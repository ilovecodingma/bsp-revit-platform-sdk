# 제안 — 관리는 SDK 로, 서버는 뜻대로

## 한 줄

**플랫폼에서 값이 나가는 자리는 «관리» 다.** 그 관리 계층을 규격과 실행 파일로 묶어
이 SDK 로 드린다. 서버는 이 규격만 맞추면 어떻게 세우셔도 된다.

---

## 1. 드리는 것 — 클라이언트 관리 계층 (완성)

| 것 | 상태 |
|---|---|
| `bsp-launcher.exe` — 백그라운드 매니저 | 동작 확인 (받기·해시검증·배치·큐·기록·메모리 감시) |
| `BSP.Platform.Entry.dll` — 리빗 엔트리포인트 | 동작 확인 (점화·세션표시·상태) |
| `bsp-verify.exe` — 정적 적합성 검사기 | 동작 확인 (5개 항목) |
| `SPEC\` 4장 — 계약·프로세스·콜백·API | 확정본 |
| `samples\HelloTool` — 최소 도구 | 빌드·검사까지 확인 |

이 다섯이 «난립을 제어한다» 의 실체다.
누가 무엇을 언제 깔았고, 무엇이 얼마나 쓰였고, 누가 리빗을 죽였는지가 전부 남는다.

## 2. 맡기는 것 — 서버

동봉한 파이썬 서버는 **참조본**이다. 빠른 확인용으로 만든 것이고, 운영 선택은 그쪽 몫이다.
스프링이든 노드든 상관없다. **`SPEC\API.md` 만 지키면 현장 PC 는 아무 영향이 없다.**

지켜야 할 것은 사실상 넷뿐이다.

1. `GET /manifest.json` — 제품·버전·파일 목록과 **SHA-256**
2. `GET /assets/<id>/<version>/<path>` — 파일
3. `GET /api/member?tenant=` — 이 회사가 받을 수 있는 목록 (없으면 아무것도 안 깐다)
4. `POST /api/usage` · `POST /api/report` — 기록 받기

심사·번들·사람·세션은 그 위에 얹는 이야기다. 없어도 배포는 돈다.

## 3. 왜 프로세스를 하나 더 두는가

관리 기능을 리빗 안에 두면 «리빗이 켜져 있는 동안만» 관리된다.
업데이트도, 기록도, 사고 지목도 전부 리빗의 수명에 묶인다.

```
        Revit.exe ──점화──▶ bsp-launcher.exe ──HTTP──▶ 서버
           │                      ▲
           └─── 파일(%AppData%\BSP\Platform) ───┘
```

- 리빗이 죽어도, 리빗이 아예 없어도 매니저는 산다
- 리빗은 서버와 직접 통신하지 않는다 (보안 심사·회선 장애에 유리)
- 업데이트는 **리빗 켜질 때 받아두고, 꺼질 때 적용**한다
  — .NET Framework 는 어셈블리를 내릴 수 없고, 리빗이 켜진 동안 DLL 은 잠긴다.
  제약을 숨기지 않고 «받기» 와 «갈아끼우기» 를 갈라 설계했다

실측·예외 처리는 `SPEC\PROCESS.md` 6장에 그대로 적어 두었다.

## 4. 먼저 정해 주셔야 하는 것 하나

**코드 서명 인증서.** 지금은 우리 플러그인조차 검사에서 FAIL 이다.

서명이 없으면 리빗이 «서명 없는 애드인» 대화상자를 띄우고 **답할 때까지 멈춘다**
(실측 1시간 45분). 무인·전사 배포가 성립하지 않는다.
누구 이름으로 서명할지 — 회사 하나로 갈지, 공급사별로 갈지 — 가 심사 정책의 출발점이다.

## 5. 이번 범위에서 뺀 것

- **AI·MCP 연계** — 나중 이야기. 규격이 파일·HTTP 두 갈래뿐이라 붙여도 구조는 안 바뀐다
- **라이선스 서버** — 회원 확인(`/api/member`) 하나로 갈음했다. 필요해지면 그때 얹는다
- **실기 러너 자동화** — 정적 검사는 돈다. 연도별 리빗 실행 검사는 아직 수동이다

---

### 요약

클라이언트(관리 계층)는 완성해서 드린다. 서버는 규격만 맞추면 원하시는 대로.
정해 주실 것은 **서명 정책** 하나다.
